One-toMany: Bidirectional
Relationship is defined on both sides
👉 Both entities know about each other
IMP: mappedBy means not creating FK

Parent reference to child
Each child reference to Parent.

Child-->> owing side

Inverse side:(No FK created)
FK is created in table
Holds object reference of owing entity.

Owning side:(Contains the FK)
we keep @JoinColumns, which holds the FK.
Holds the FK relationship in a table.


package OneToOne;

import java.util.ArrayList;
import java.util.List;

import jakarta.persistence.CascadeType;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.JoinColumns;
import jakarta.persistence.OneToMany;
import jakarta.persistence.OneToOne;
import jakarta.persistence.Table;

@Entity
@Table(name = "useru")
public class UserDetail {
	@Id
	@GeneratedValue(strategy = GenerationType.SEQUENCE)
	long uid;
	@Column(name = "Username", unique = true)
	String uname;
	int uage;
	
	@OneToMany(mappedBy = "user")
	private List<Orderrr> order=new ArrayList();

	
	public UserDetail() {
		
	}

	public long getUid() {
		return uid;
	}

	public void setUid(long uid) {
		this.uid = uid;
	}

	public String getUname() {
		return uname;
	}

	public void setUname(String uname) {
		this.uname = uname;
	}

	public int getUage() {
		return uage;
	}

	public void setUage(int uage) {
		this.uage = uage;
	}

	public List<Orderrr> getOrder() {
		return order;
	}

	public void setOrder1(List<Orderrr> order) {
		this.order = order;
	}

}








package OneToOne;
import OneToOne.UserDetail;
import jakarta.persistence.CascadeType;
import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.ManyToOne;
import jakarta.persistence.Table;


@Entity
@Table(name = "orderu")
public class Orderrr {
	
	@Id
	@GeneratedValue(strategy = GenerationType.AUTO)
	long id;
	String ordername;
	public Orderrr() {
		
	}

	@ManyToOne(cascade = CascadeType.ALL)
	@JoinColumn(referencedColumnName = "uid")
	UserDetail user;
	public long getId() {
		return id;
	}
	public String getOrdername() {
		return ordername;
	}
	public void setId(long id) {
		this.id = id;
	}
	public void setOrdername(String ordername) {
		this.ordername = ordername;
	}
	public void setUser(UserDetail userDetail) {
		// TODO Auto-generated method stub
		
	}

}








